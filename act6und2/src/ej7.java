public class ej7 {

    public static void main(String[] args) {

        int contador;

        for (contador = 10; contador >= 0; --contador){
            System.out.println("contador es: " + contador);
        }
        System.out.println("\nBucle acaba. \nContador ahora es" + contador);

        // en la primera opción sale que contador es -1
        // en la segunda sale lo mismo: contador es -1
    }

    /*
    8.
    a) No se puede omitir pero se podría hacer fuera del for, en el cuerpo del bucle
    b) Las partes de inicializador y condición no se pueden omitir

    */
}
