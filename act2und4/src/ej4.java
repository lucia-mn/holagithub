/*public class ej4 {

    //EJ2
    public static void main(String[] args) {

        //PRIMERA FORMA
        int [] array2 = array2.clone();
        int[] array;

        arrays.sort (array2); //ordenas el array de menor a mayor
        System.out.printf(array2 [array.length-1]); //el máximo
        System.out.printf(array2 [0]); //el mínimo


        //SEGUNDA FORMA
        int max = array[0];
        for (int i = 0; i < array.length; i++) {

            if (array[i] > max) {
                max = array[i];
            }
        }
        // para el mínimo hay que poner en vez de max un min

        //PARA IMPRIMIR ARRAYS
        for (int i = 0; i < array.length; i++) {
            System.out.print(array[i] + " ");
        }

        for (int i: array) {
            System.out.print(i + " ");
        }

        System.out.print(arrays.toString(array));
        //de la función arrays pasamos el array a string!!!

    }

    //EJ5
    //CAMBIAR EL J<=6 POR J<= Pago semanal.lenght
}*/
